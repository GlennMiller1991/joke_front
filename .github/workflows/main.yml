name: Main

on:
  push:
    branches:
      - main

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run:  docker build -t joke-front .

      - name: Save image as archive
        run:  docker save joke-front -o ${{ vars.ARCHIVE_NAME }}

      - name: Checkout archive
        run: ls -lh ${{ vars.ARCHIVE_NAME }}

      - name: Set archive permissions
        run: chmod 644 ${{ vars.ARCHIVE_NAME }}

      - name: Replace archive on the VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_KEY }}
          source: ${{ vars.ARCHIVE_NAME }}
          target: /home/${{ secrets.VPS_USER_SECONDARY }}/apps/joke/front

      # - name: Deploy on the VPS
      #   uses: appleboy/ssh-action@v1.0.3
      #   with: 
      #     host: ${{ secrets.VPS_HOST }}
      #     username: ${{ secrets.VPS_USER }}
      #     password: ${{ secrets.VPS_KEY }}
      #     script: |
      #       docker load -i /home/${{ secrets.VPS_USER_SECONDARY }}/apps/joke/front/${{ vars.ARCHIVE_NAME }}
      #       if [ "$(docker ps -q -f name=joke-front)"]; then
      #         docker stop joke-front
      #       fi
      #       if [ "$(docker ps -a -q -f name=joke-front)"]; then
      #         docker rm joke-front
      #       fi
      #       docker run --name joke-front -p4001:4000 -d --rm joke-front